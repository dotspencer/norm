<% var links = ["Send", "Login", "Sign Up", "Settings"]  %>

<nav>
  <div class="logo">Review Norm</div>
  <ul>
    <% for(var i = 0; i < links.length; i++){ %>
      <% var l = links[i]; %>
      <li><a href="<%= l.split(' ').join('').toLowerCase() %>"><%= l %></a></li>
    <% } %>
  </ul>
</nav>
