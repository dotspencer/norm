<nav>
  <div class="logo">Review <span class="red">Norm</span></div>
  <ul>
    <% for (var i = 0; i < navData.length; i++) { %>
      <% var d = navData[i];  %>
      <li><a href="<%= d.link %>"><%= d.title %></a></li>
    <% } %>
  </ul>
</nav>
